/**
 * main function
 * @param {function}  callback    the callback function
 */
const main = (callback) => {
  console.log('Task 0269 - Alien Dictionary:');
  [
  ].forEach( vs => {
  });
  if (callback) {
    callback();
  }
};


module.exports = main;
